using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Combineable : MonoBehaviour
{
    //Check if the item player is holding can be combined with item on the counter. If yes, put both props together OR destroy both and spawn new prefab in place.
}
